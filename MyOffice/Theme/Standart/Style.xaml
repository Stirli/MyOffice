<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:MyOffice.Theme.Standart">
    <Style x:Key="ControlBaseStyle" TargetType="Control">
        <Setter Property="Margin" Value="4"/>
        <Setter Property="Padding" Value="4"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="FontFamily" Value="Segoe UI"></Setter>
    </Style>
    <Style x:Key="TextBlockBaseStyle" TargetType="TextBlock">
        <Setter Property="Margin" Value="4"/>
        <Setter Property="Padding" Value="4"/>
        <Setter Property="FontSize" Value="16"/>
        <Setter Property="FontFamily" Value="Segoe UI"></Setter>
    </Style>
    <Style x:Key="TextBoxBaseStyle" TargetType="TextBox"  BasedOn="{StaticResource ControlBaseStyle}">
    </Style>

    <Style TargetType="Button" BasedOn="{StaticResource ControlBaseStyle}">
        <Setter Property="Background" Value="#FF007400"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderThickness" Value="0"/>
    </Style>
    <Style TargetType="TextBlock" BasedOn="{StaticResource TextBlockBaseStyle}">
    </Style>
    <Style TargetType="TextBox"  BasedOn="{StaticResource TextBoxBaseStyle}">
    </Style>
    <Style TargetType="DatePicker" BasedOn="{StaticResource ControlBaseStyle}"></Style>
    
</ResourceDictionary>